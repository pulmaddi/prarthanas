# Ensure shell scripts always use LF line endings in the repo,
# regardless of the developer's platform (prevents CRLF shebang errors on Linux).
*.sh text eol=lf

# Text files: normalise to LF in the repo, checkout as-is on each platform.
* text=auto
