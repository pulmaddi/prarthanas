# Dependencies / build (for later app phases)
node_modules/
dist/
build/
.expo/

# Env & secrets — never commit real env, but DO track the examples
.env
.env.*
!.env.example
!**/.env.example
*.pem
*.key

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Expo prebuild native projects (regenerate with `expo prebuild`)
apps/mobile/android/
apps/mobile/ios/
